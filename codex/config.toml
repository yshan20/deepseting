# Managed defaults installed by deepseting.
# Machine-specific plugins, MCP servers and trusted projects stay in the local config.
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
approval_policy = "on-request"
web_search = "live"
personality = "friendly"
