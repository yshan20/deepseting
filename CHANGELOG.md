# История изменений

## Unreleased

Added external orchestration compatibility and Game Master Plan project routing.

- Обычные проекты сохраняют STANDARD_DEEPSETING и утверждение критериев.
- Game Master Plan использует внешнюю Stage/Wave/Lab оркестрацию без параллельной spec/progress иерархии.
- Добавлены границы автономности/approval и совместимость с durable filesystem project memory.
- Генератор пропускает конфликтующий каркас; новые hooks маршрутизируют по индексу.
- Повторная инициализация сохраняет существующие спецификации; установка сохраняет локальные изменения навыка.
- Добавлены интеграционные тесты определения режима, инициализации и реальных Git-коммитов.

Вычищены дубли: то, что Claude Code делает сам, и повторы одного текста.

- Правила разложены по `~/.claude/rules/`; `CLAUDE.md` остался коротким ядром.
- Усилие рассуждения задаётся ключом `effortLevel`; `autoUpdatesChannel: latest` убран как значение по умолчанию.
- Правило определения режима описано в одном файле, остальные ссылаются на него.
- Убраны инструкции, дублирующие встроенное поведение Claude Code.
- Резервные копии переехали в `~/.claude/backups/`: копия навыка в `skills/` загружалась
  Claude Code как ещё один скилл.

В репозитории пока нет нумерованных релизов, тегов или файла версии; новая схема нумерации не вводится.
