---
name: project-specifications
description: Когда пользователь просит «давай сделаем», «добавь возможность», «начинаем проект», «продолжим вчерашнее», «что по прогрессу», «закрываем функцию» или «обнови спеку», а также при старте нового проекта, возврате к проекту после перерыва и закрытии завершённой функции — помогает вести спецификации проекта — центральная SPEC, спецификация функции с критериями готовности, план реализации и файл прогресса.
---

# Спецификации проекта

Проект ведётся по четырём документам на русском. `README.md` в корне описывает структуру проекта; спецификации описывают систему, границы, решения и ход работ. Это разные роли — не дублируй их.

## Документы и их роли

| Документ | Путь | Что внутри | Кто читает |
| --- | --- | --- | --- |
| Центральная спецификация | `docs/SPEC.md` | Что за система, границы, ключевые решения | Все агенты, всегда |
| Спецификация функции | `docs/features/<имя>.md` | Что делаем сейчас, критерии готовности | Агент на задаче |
| План реализации | `docs/plan.md` | Пункты в нужном порядке | Агент, по одному пункту |
| Файл прогресса | `PROGRESS.md` | След работы: сделано / заблокировано / пропущено / решения без указания | Агент пишет, человек читает |

`PROGRESS.md` — это след работы, а не приёмка. Состояние системы проверяется отдельно, в чистой сессии, по критериям готовности из `docs/features/<имя>.md`, а не чтением `PROGRESS.md`.

## Структура документов

### `docs/SPEC.md`
- **Назначение** — что за система, какую проблему решает.
- **Границы** — что входит / что НЕ входит.
- **Ключевые решения** — таблица: решение → причина → рассмотренные альтернативы.
- **Ограничения и контекст** — внешние зависимости, жёсткие ограничения.

### `docs/features/<имя>.md`
- **Цель** — что именно делаем сейчас, одним предложением.
- **Область** — что входит / не входит в эту функцию.
- **Критерии готовности** — таблица с колонкой «Чем проверяется»: каждая строка — состояние, которое должно стать правдой, и способ это проверить. Пустая правая колонка означает, что критерий сформулирован плохо.
- **Связь со SPEC** — на какие решения/границы из `docs/SPEC.md` опирается.

### `docs/plan.md`
- Заголовок с именем текущей функции.
- **Пункты** — таблица в нужном порядке с колонкой «Чем проверяется»; у каждого пункта: что сделать + критерий готовности пункта + чем проверяется.

### `PROGRESS.md`
- **Сделано** — что завершено (со ссылкой на пункты плана).
- **Заблокировано** — что и чем заблокировано (сюда же — тесты, которые кажутся неверными).
- **Пропущено** — что и почему пропущено.
- **Решения без указания** — что решено самостоятельно, чего не было в спецификации.

## Порядок работы

1. **Старт проекта** — создай центральную спецификацию `docs/SPEC.md` (каркас генерируется командой `pwsh -File ~/.claude/bin/new-project.ps1 <путь-к-проекту>`).
2. **Новая функция** — до начала кода создай `docs/features/<имя>.md` с критериями готовности.
3. **Остановись и покажи критерии готовности человеку** — жди подтверждения. Критерии, написанные тем же агентом, который по ним отчитывается, не являются проверкой. Без подтверждения к плану и коду не переходи.
4. **План** — разбей работу в `docs/plan.md` на пункты в нужном порядке и выполняй строго по одному пункту за раз.
5. **Прогресс** — после каждого шага обновляй `PROGRESS.md`: что сделано, что заблокировано, что пропущено, какие решения принял без указания.

## Жизненный цикл

- `PROGRESS.md` — один на проект, непрерывный лог; историю не перезаписывай.
- `docs/plan.md` — перезаписывается под текущую функцию.
- Завершённую функцию переноси в `docs/features/done/`, чтобы в `docs/features/` оставались только актуальные.

## Шаблоны

Пустые шаблоны всех документов лежат рядом с этим скиллом, в `~/.claude/skills/project-specifications/templates/`. Каркас проекта создаёт `pwsh -File ~/.claude/bin/new-project.ps1 <путь-к-проекту>`; отдельную заготовку функции — тот же скрипт с `-Feature <имя-функции>`. Заполняй документы по структуре выше.
